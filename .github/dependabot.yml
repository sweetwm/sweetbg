version: 2
updates:
  # Keep GitHub Actions (checkout, upload-artifact, flawfinder, ...) up to date
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    commit-message:
      prefix: ci
